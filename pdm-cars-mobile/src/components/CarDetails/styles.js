import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    borderRadius: 5,
    borderColor: 'black',
    borderWidth: 2,
    padding: 5
  },
  field: {
    fontSize: 25
  },
});

export default styles;
