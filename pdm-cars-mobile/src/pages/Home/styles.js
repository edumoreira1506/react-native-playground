import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  carsList: {
    paddingTop: 10,
    height: '100%',
    width: '100%',
  },
  car: {
    marginBottom: 10,
    marginLeft: '5%',
    marginRight: '5%',
  },
});

export default styles;
